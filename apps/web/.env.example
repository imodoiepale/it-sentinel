NEXT_PUBLIC_SUPABASE_URL=https://ncyerayycwkqytznnkrs.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...   # run: node scripts/configure-project.mjs <project-ref>
NEXT_PUBLIC_CONTROL_PLANE_URL=http://localhost:8787

# The ElevenLabs Conversational AI agent the console talks to. Optional — the
# production agent id is the built-in fallback in components/VoiceAgentWidget.
# The agent must be public (or the console needs a signed-URL endpoint), and
# the browser needs https plus microphone permission to connect.
NEXT_PUBLIC_ELEVENLABS_AGENT_ID=agent_8001m15d76mafg9rgjkpyfxwm1z6

# NEXT_PUBLIC_SENTINEL_AGENT_URL was dropped along with the Ask Sentinel box:
# it addressed apps/sentinel-agent's /v1/ask, which is not deployed, and
# deployment aimed the variable at the control plane, which has no such route.
